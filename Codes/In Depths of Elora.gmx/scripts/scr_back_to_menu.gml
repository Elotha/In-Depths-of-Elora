///Back to Menu

{
    scr_clear();
    destroy(obj_HUD);
    destroy(obj_Score);
    destroy(obj_Lives);
    destroy(obj_Keys);
    destroy(obj_Camera);
    destroy(obj_Background);
    destroy(obj_System);    
}
