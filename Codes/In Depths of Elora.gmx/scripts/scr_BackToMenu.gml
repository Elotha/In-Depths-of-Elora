///Back to Menu

{
    scr_Clear();
    destroy(obj_HUD);
    destroy(obj_Camera);
    destroy(obj_Background);
    destroy(obj_System);
    destroy(obj_LightBugCreator);
    destroy(obj_CharacterLight);
}
